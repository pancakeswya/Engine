#include "backend/gl/renderer/shaders.h"

#include <GL/glew.h>

namespace gl {

std::vector<Shader> GetShaders() {
    return {
        {
            R"(@VERT@)",
            GL_VERTEX_SHADER
        },
        {
            R"(@FRAG@)",
            GL_FRAGMENT_SHADER
        }
    };
}

} // namespace gl